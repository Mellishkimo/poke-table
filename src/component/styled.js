import styled from 'styled-components'

export const BugButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #a8b820;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#a8b820'};
  cursor: pointer;
  background: ${props => props.selected ? '#a8b820' : 'white'};
`

export const DragonButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #5a3696;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#5a3696'};
  cursor: pointer;
  background: ${props => props.selected ? '#5a3696' : 'white'};
`

export const ElectricButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #f8d030;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#f8d030'};
  cursor: pointer;
  background: ${props => props.selected ? '#f8d030' : 'white'};
`

export const FightingButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #c03028;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#c03028'};
  cursor: pointer;
  background: ${props => props.selected ? '#c03028' : 'white'};
`

export const FireButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #f08030;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#f08030'};
  cursor: pointer;
  background: ${props => props.selected ? '#f08030' : 'white'};
`

export const FlyingButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #a890f0;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#a890f0'};
  cursor: pointer;
  background: ${props => props.selected ? '#a890f0' : 'white'};
`

export const GhostButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #705898;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#705898'};
  cursor: pointer;
  background: ${props => props.selected ? '#705898' : 'white'};
`

export const GrassButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #78c850;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#78c850'};
  cursor: pointer;
  background: ${props => props.selected ? '#78c850' : 'white'};
`

export const GroundButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #e0c068;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#e0c068'};
  cursor: pointer;
  background: ${props => props.selected ? '#e0c068' : 'white'};
`

export const IceButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #98d8d8;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#98d8d8'};
  cursor: pointer;
  background: ${props => props.selected ? '#98d8d8' : 'white'};
`

export const NormalButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #a8a878;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#a8a878'};
  cursor: pointer;
  background: ${props => props.selected ? '#a8a878' : 'white'};
`

export const PoisonButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #a040a0;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#a040a0'};
  cursor: pointer;
  background: ${props => props.selected ? '#a040a0' : 'white'};
`

export const PsychicButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #f85888;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#f85888'};
  cursor: pointer;
  background: ${props => props.selected ? '#f85888' : 'white'};
`

export const RockButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #b8a038;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#b8a038'};
  cursor: pointer;
  background: ${props => props.selected ? '#b8a038' : 'white'};
`

export const WaterButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #6890f0;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#6890f0'};
  cursor: pointer;
  background: ${props => props.selected ? '#6890f0' : 'white'};
`

export const BasicButton = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #cc7a00;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#cc7a00'};
  cursor: pointer;
  background: ${props => props.selected ? '#cc7a00' : 'white'};
`

export const Stage1Button = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #808080;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#808080'};
  cursor: pointer;
  background: ${props => props.selected ? '#808080' : 'white'};
`

export const Stage2Button = styled.button`
  font-size: 15px;
  font-weight: bold;
  padding: 4px;
  border: 1.5px solid #d4af37;
  border-radius: 6px;
  margin-left: 5px;
  margin-right: 5px;
  min-width: 68px;
  outline: none;
  color: ${props => props.selected ? 'white' : '#d4af37'};
  cursor: pointer;
  background: ${props => props.selected ? '#d4af37' : 'white'};
`

export const RankSelect = {
  container: (provided, state) => ({
    ...provided,
    margin: 'auto',
    marginLeft: '10px',
    marginTop: '17px',
    width: '231px'
  }),
  indicatorSeparator: state => ({
    display: 'none',
  }),
}

export const ComparisonSelect = {
  container: (provided, state) => ({
    ...provided,
    margin: 'auto',
    marginLeft: '10px',
    marginTop: '17px',
    width: '137px'
  }),
  indicatorSeparator: state => ({
    display: 'none',
  }),
}